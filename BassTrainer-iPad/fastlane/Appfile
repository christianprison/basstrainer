# fastlane Appfile — BassTrainer iPad
# Werte kommen aus Umgebungsvariablen / GitHub-Secrets (siehe fastlane/README.md).

app_identifier(ENV["APP_IDENTIFIER"] || "com.basstrainer.app")

# Developer-Portal-Team. Authentifizierung läuft über den App Store Connect
# API-Key (siehe Fastfile), daher kein apple_id nötig.
team_id(ENV["APPLE_TEAM_ID"]) if ENV["APPLE_TEAM_ID"]
